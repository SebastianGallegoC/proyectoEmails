# ============================================
# VARIABLES DE ENTORNO - PLANTILLA
# ============================================
# Este archivo es una PLANTILLA que muestra qué variables necesitas configurar.
# NO contiene valores reales y es seguro subirlo a Git.
#
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a .env
# 2. Reemplaza los valores de ejemplo con tus credenciales reales
# 3. El archivo .env NO se subirá a Git (está en .gitignore)
# ============================================

# --- Base de Datos PostgreSQL ---
POSTGRES_HOST=postgres
POSTGRES_DB=ConexionCRUDWalter
POSTGRES_USER=walteresvc
POSTGRES_PASSWORD=your-password-here
POSTGRES_PORT=5432

# Cadena de conexión completa
# Para desarrollo local SIN Docker: usa 'localhost' en lugar de 'postgres'
# Para Docker: usa 'postgres' (nombre del contenedor)
CONNECTION_STRING=Host=postgres;Database=ConexionCRUDWalter;Username=walteresvc;Password=your-password-here

# --- JWT (Autenticación) ---
# Usa una clave de al menos 32 caracteres
JWT_KEY=your-secret-key-min-32-characters-long
JWT_ISSUER=EmailsPAPI
JWT_AUDIENCE=EmailsPUsers

# --- SMTP (Gmail) ---
# ⚠️ Usa "App Password" de Google, NO tu contraseña normal
# Crear App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_STARTTLS=true
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# --- Aplicación ---
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:8080
API_PORT=5000
