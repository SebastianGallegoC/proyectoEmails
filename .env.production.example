# ============================================
# VARIABLES DE ENTORNO - PRODUCCIÓN
# ============================================
# ⚠️ ARCHIVO DE EJEMPLO - NO USAR DIRECTAMENTE
# 
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a .env.production
# 2. Reemplaza TODOS los valores con tus credenciales REALES de producción
# 3. NUNCA subas .env.production a Git
# 4. En el servidor, usa: docker-compose --env-file .env.production up -d
# ============================================

# --- Base de Datos PostgreSQL (Producción) ---
# Usa credenciales DIFERENTES a las de desarrollo
POSTGRES_HOST=postgres
POSTGRES_DB=EmailsP_Production
POSTGRES_USER=emailsp_prod_user
POSTGRES_PASSWORD=CAMBIAR_POR_PASSWORD_FUERTE_ALEATORIO
POSTGRES_PORT=5432

# Para BD externa (ej: AWS RDS, Azure Database, etc.)
CONNECTION_STRING=Host=tu-servidor-produccion.com;Database=EmailsP_Production;Username=emailsp_prod_user;Password=CAMBIAR_PASSWORD

# --- JWT (Autenticación) ---
# ⚠️ CRÍTICO: Genera una clave COMPLETAMENTE NUEVA y ALEATORIA
# Usa al menos 64 caracteres. Puedes generarla con:
# openssl rand -base64 64
JWT_KEY=CAMBIAR_POR_CLAVE_ALEATORIA_MUY_LARGA_MINIMO_64_CARACTERES
JWT_ISSUER=EmailsPAPI
JWT_AUDIENCE=EmailsPProductionUsers

# --- SMTP (Gmail) ---
# ⚠️ Usa una cuenta dedicada para producción
# NUNCA uses tu cuenta personal
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_STARTTLS=true
SMTP_USER=produccion@tuempresa.com
SMTP_PASSWORD=CAMBIAR_POR_APP_PASSWORD_GMAIL

# --- Aplicación ---
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080
API_PORT=80

# --- Seguridad adicional (opcional) ---
# Dominio de tu aplicación
ALLOWED_HOSTS=tudominio.com,www.tudominio.com

# CORS (limita los orígenes permitidos en producción)
CORS_ORIGINS=https://tudominio.com,https://app.tudominio.com
